spring:
  application:
    name: SpringAiOpen
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
    chat:
      client:
        enabled: false
      memory:
        repository:
          jdbc:
            initialize-schema: always
  datasource:
    url: jdbc:h2:file:C:\Users\jamal\h2History;AUTO_SERVER=true
    driver-class-name: org.h2.Driver
    username: yuosef
    password: 12345


logging:
  pattern:
    console: ${LOGPATTERN_CONSOLE:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n}
  level:
    org:
      springframework:
        ai:
          chat:
            client:
              advisor: DEBUG